---
type: task
id: M12-T1
module: M12
depends_on: [M10-T2, M11-T1, M11-T2]
unlocks: [M12-T2]
edges: [E-31]
est_days: 1.5
branch: task/M12-T1
status: merged
---

# M12-T1　维护聚蚁品牌、主题与仪表盘增强

- 所属模块：[[M12]]　聚蚁品牌层：品牌首页、主题字体、蚁群动效、仪表盘增强、ext 外链和禁自更新边界
- 前置依赖：[[M10-T2]]、[[M11-T1]]、[[M11-T2]]
- 完成后解锁：[[M12-T2]]

## 输入 / 产出

- **输入**：public settings、theme/font preference
- **产出**：JuyiHome、动效、品牌组件与 Dashboard 插入

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) `home_content` 优先且动效支持 reduced-motion/隐藏降级（[[E-31]]）
2) 品牌色不覆盖平台/状态语义色
3) 新能力优先放聚蚁新文件

## 必须处理的边界

- [[E-31]]　管理员首页覆盖与动效降级
    - 触发：`home_content` 非空、reduced-motion、页面隐藏或低性能环境
    - 期望：显式首页覆盖优先；蚁群动效暂停/降级，核心内容和导航仍可用

## 代码位置

<!-- code:begin -->
- `frontend/src/views/HomeView.vue` — home_content 覆盖与 JuyiHome 壳（E-31）
- `frontend/src/views/home/JuyiHome.vue` — 聚蚁默认落地页（2026-08 动效层：入场编排/模型跑马灯/不对称 bento+微演示/品牌宣言暗面板/滚动浮现 data-jy-reveal；背景层改 fixed 视口固定，画布不随页高拉大）
- `frontend/src/components/juyi/JuyiSwarmField.vue` — 蚁群引擎 v4（替代 v2 Canvas，原 JuyiSwarmCanvas.vue 已删）：SVG 手绘虫（四节躯体+腹柄结节/六足交替三角步态/膝状触角/地面投影/渐隐尾迹）三景深驮光粒归巢，巢房闪光+WAAPI 脉冲环；逐帧只写 transform、30fps 节流、无画布重绘/无 DPR 放大；reduced-motion 输出静态构图。注意：<use> shadow DOM 与 JS 动态节点的样式必须放非 scoped 样式块，且选择器进不去 shadow tree，每只蚁的步频/相位只能靠 CSS 自定义属性继承穿透
- `frontend/src/composables/useJuyiAppearance.ts` — 主题字体持久化
- `frontend/src/components/home/AntTrailPipeline.vue` — 管线 v2：数据颗粒 rAF 汇流、入巢脉冲、出口流出、描画入场、悬停高亮（path 元素与长度挂载时缓存）
- `frontend/src/components/juyi/HiveAccountGrid.vue` — 蜂窝格错落浮现（jy-hex-in + 逐格 delay）
- `frontend/src/components/juyi/UserEndpointHero.vue` — 问候区入场编排 + 复制成功对勾弹出（jy-pop）
- `frontend/src/components/layout/AuthLayout.vue` — 登录卡升起入场+指针微倾斜（±2.5°,仅精确指针）、蚁标轨道环 jy-orbit
- `frontend/src/components/layout/AppLayout.vue` — 主列 margin 276/92px（浮动侧边栏 10px 间隙，≥lg）
- `frontend/src/components/layout/AppHeader.vue` — juyi-float-header 悬浮圆角顶栏挂点
- `frontend/src/styles/juyi.css` — 动效层 v2：jy-rise/jy-reveal/jy-sweep/jy-marquee/jy-manifesto/jy-demo-dot/jy-wave-cell/jy-auth-card/jy-orbit/jy-pop/jy-hex-in/jy-provider + 浮动侧边栏与顶栏（≥lg,不加 overflow:hidden 保弹层）+ 侧栏激活指示条 + reduced-motion 全量冻结
- `frontend/src/directives/juyiCount.ts` — KPI 数字滚动指令 v-jy-count（IO 触发、reduced-motion 直出终值）
- `frontend/src/composables/useJuyiReveal.ts` — 滚动浮现兵装（JS 武装后才隐藏，无 JS/reduced-motion 不隐藏）
- `frontend/src/main.ts` — 注册 v-jy-count 指令（2 行插入）
- `frontend/src/i18n/locales/{zh,en}/juyi.ts` — 新增 juyi.home.manifesto.* 文案
- `frontend/src/views/admin/DashboardView.vue` — 4 张核心 KPI 卡接 v-jy-count
- `frontend/src/components/layout/__tests__/docUrlSanitization.spec.ts` — 修复存量漂移：HomeView 壳化后断言改指 JuyiHome
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
管理员 home_content 是显式更高优先级，聚蚁首页只是默认渲染面。动效读取主题变量并在 reduced-motion/页面隐藏时降级；主题只覆盖品牌交互位，平台身份和状态语义颜色保持稳定。

2026-08 动效层 v2（finesse 流程，用户确认 A+B 方向后落地）：①蚁群引擎升级为 2.5D（三景深视差+分段躯体+驮光粒归巢+入巢脉冲），性能靠粒子封顶 90/精灵图辉光（禁逐帧 shadowBlur）/DPR≤2/三重暂停兜底；主页背景层改 fixed 后画布从整页高变视口尺寸，内存与 iOS canvas 面积限制风险同时消除。②主页动效预算：入场浮起、管线颗粒汇流（rAF+getPointAtLength，元素与长度缓存）、模型跑马灯（全站唯一 marquee）、bento 两张主演示卡（多源归一颗粒流+八格负载波）不对称破除 6 连同卡、暗色宣言面板。③滚动浮现采用 JS 武装模式：data-jy-reveal 元素先由 composable 加 .jy-reveal 再由 IO 放行 .jy-in，无 JS/reduced-motion 永不隐藏内容（Playwright 实测滚动后 18/18 放行）。④后台壳改浮动侧边栏+悬浮顶栏（≥lg），侧栏不加 overflow:hidden 以免裁剪主题菜单弹层；主列 margin 与侧栏宽度联动改 276/92px。⑤v-jy-count 数字滚动是纯展示指令，模板里数字插值改由指令渲染避免双写。⑥回填修复 docUrlSanitization.spec 存量漂移（HEAD 的 HomeView 本无 sanitizeUrl，壳化时漏改）。

2026-08 v3 反馈迭代（用户验收不满意：蚂蚁太小/页面卡/不够冲击/像模板）：⑦引擎从 Canvas 换轨 SVG（JuyiSwarmField.vue）：蚂蚁改为手绘虫（径向渐变躯体着色+地面投影+尾迹随组变换零额外开销），16 只大尺寸三景深替代 90 只尘埃点，逐帧仅写 style.transform 且 30fps 节流；性能採样 3 秒 0 long task。真正的卡顿源是 backdrop-filter 叠在动画层上——登录卡/bento/标签/平台墙全部去 blur 改实底，这是比换引擎更大的收益。⑧色彩律动：hero 关键词流动渐变字（i18n 拆 titleA/B 实现，每页最多一处动态渐变）、登录页双漂移色斑、暗色宣言面板大字+静态渐变句、CTA 呼吸辉光、主页色带分区+行军蚁分隔线。⑨去模板味：hero 分行大字堆叠、宣言双行渐变句、bento 顶部发丝线悬停拉丝、管线下颗粒加大+悬停路径加速。⑩登录卡隐喻：巢房对准卡片正后方，蚂蚁归巢=登录。⑪教训：<use> shadow DOM 与 JS 动态创建的 SVG 节点都拿不到 Vue scoped 样式标记，必须用非 scoped 样式块（jy- 前缀防撞）；SMIL 动画无法用 CSS 暂停，reduced-motion 降级只能 display:none 隐藏运动元素。

2026-08 v4 步态迭代（用户验收：蚂蚁在页面里是「滑行」不是走，且缺触角）：⑫六足改交替三角步态（tripod A=左前/右中/左后 与 B 反相半周期），每条腿包一层 `translate(基节)` 的 `<g>`、内层 path 从 (0,0) 起笔并设 `transform-box: fill-box` 才能绕基节旋转——SVG 元素的 `transform-origin` 默认参照整个 viewBox，不设 fill-box 腿会绕画面中心甩飞。⑬**步频必须由线速度反推**（`applyGait`：周期 = 步幅 ÷ 线速度），拍脑袋定常数一定打滑；且速度模型要从「每秒推进多少 t」改成恒定线速度，因为 9 条贝塞尔路径弧长从 ~550 差到 ~1800，按 t 匀速会让短路上的线速度只有长路的三分之一，步频跟着塌掉。⑭步频不是越高越真：首版按 8-11Hz 落地，用户反馈「太快、有点抽象」——每周期只有 5-7 帧，人眼读到的是高频抖动而非迈步。降到 ~4.2Hz（每周期 14 帧）才看得出迈步；但步频压下来必须同步放大步幅（腿加长到距基节 ~10 单位、摆幅 22°→28°）与蚁体 scale，否则慢步频 × 小步幅 = 线速度掉到 25 单位/秒，蚂蚁看着像钉在原地——这三者是一组连动参数，改一个就要重算另两个。⑮「看着抽象」的主因不是腿，而是**驮的光粒光晕半径过大**（5.6，随蚁体放大后把整个上半身罩成亮斑，虫的轮廓全糊了），收到 3.4 后形态立刻清晰。⑯六足角度不能均匀放射（会立刻读成蜘蛛），要前足朝前/中足侧展/后足朝后的纺锤轮廓；补腹柄结节（蚂蚁与其他昆虫的分界特征）、触角重画为膝状并加长到超过头+胸——小尺寸下触角是「这是只蚂蚁」的第一识别特征，原来 4 单位长几乎不可见。⑰CSS 步态动画不归 rAF 管：`stop()` 只停位移，六足会继续空转烧 CPU，而 `animation-play-state` 不可继承、选择器又进不去 `<use>` shadow tree，只能用自定义属性 `--jy-anim` 中转暂停。⑱验证手法：`document.getAnimations()` 拿不到 `<use>` shadow tree 内的动画（只能看到 `<defs>` 里那份模板），要证明步态真的在跑，得冻结 rAF 把蚂蚁钉住再连拍两帧比对腿姿。⑲复活了 `.jy-ant-body` —— 该起伏动画自 v2 起就写在样式里但从未挂到任何元素上。
<!-- notes:end -->

---

维护基线 `custom` @ `sub2api-private`　建议分支 `task/M12-T1`
